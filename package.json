{
  "name": "@buoy/workspace",
  "version": "0.1.1",
  "description": "Buoy — a durable remote terminal (ssh + tmux) whose sessions resurface after network drops",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "vite",
    "build:frontend": "npm run typecheck:ui && vite build",
    "build:ui-test": "tsc -p tsconfig.ui-test.json",
    "typecheck:ui": "tsc -p tsconfig.ui.json",
    "typecheck:test": "tsc -p tsconfig.test.json",
    "typecheck:ui-test": "tsc -p tsconfig.ui-test.json --noEmit",
    "typecheck": "npm run typecheck:ui && npm run typecheck:test && npm run typecheck:ui-test",
    "test": "tsx --test test/*.test.ts",
    "test:ui:build": "npm run build:frontend && npm run build:ui-test && cross-env TAURI_CONFIG={\\\"build\\\":{\\\"devUrl\\\":null}} cargo build --manifest-path src-tauri/Cargo.toml --features ui-test",
    "test:ui": "npm run test:ui:build && wdio run wdio.conf.ts",
    "gui-rename": "npm run test:ui:build && wdio run wdio.conf.ts --spec test/gui-rename.ts",
    "gui-reorder": "npm run test:ui:build && wdio run wdio.conf.ts --spec test/gui-reorder.ts",
    "gui-notifications": "npm run test:ui:build && wdio run wdio.conf.ts --spec test/gui-notifications.ts",
    "gui-new-session": "npm run test:ui:build && wdio run wdio.conf.ts --spec test/gui-new-session.ts",
    "gui-terminal-repaint": "npm run test:ui:build && wdio run wdio.conf.ts --spec test/gui-terminal-repaint.ts",
    "gui-session-history": "npm run test:ui:build && wdio run wdio.conf.ts --spec test/gui-session-history.ts",
    "gui-mobile-shell": "npm run test:ui:build && wdio run wdio.conf.ts --spec test/gui-mobile-shell.ts",
    "measure:renderer": "npm run test:ui:build && cross-env BUOY_RENDERER_BENCHMARK_BACKEND=canvas wdio run wdio.conf.ts --spec test/gui-renderer-benchmark.ts && cross-env BUOY_RENDERER_BENCHMARK_BACKEND=dom wdio run wdio.conf.ts --spec test/gui-renderer-benchmark.ts",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:test": "cargo test -p buoy --lib",
    "mobile:check": "npm run typecheck && npm run build:frontend -w @buoy/mobile && cargo check -p buoy-mobile",
    "mobile:test": "cargo test -p buoy-mobile --lib",
    "mobile:ios:init": "npm run ios:init -w @buoy/mobile",
    "mobile:ios:dev": "npm run ios:dev -w @buoy/mobile",
    "mobile:android:init": "npm run android:init -w @buoy/mobile",
    "mobile:android:dev": "npm run android:dev -w @buoy/mobile"
  },
  "license": "MIT",
  "//devDependencies": "The GUI suites use WebdriverIO's embedded Tauri WebDriver to exercise the real platform webview; the ui-test Cargo feature is excluded from production builds.",
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/node": "^24.0.0",
    "@wdio/cli": "^9.30.1",
    "@wdio/local-runner": "^9.30.1",
    "@wdio/mocha-framework": "^9.30.1",
    "@wdio/spec-reporter": "^9.30.1",
    "@wdio/tauri-service": "^1.3.0",
    "cross-env": "^10.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
